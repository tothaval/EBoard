<UserControl x:Class="EBoardSDK.Controls.Area.AreaVerticalOuterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EBoardSDK.Controls.Area"
             d:DataContext="{d:DesignInstance Type=local:AreaVerticalOuterViewModel`1}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <ListBox MinWidth="100" MinHeight="25"
             ItemsSource="{Binding HorizontalInnerViewModels}"
             HorizontalContentAlignment="Stretch"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             VerticalContentAlignment="Stretch">
        <ListBox.ItemsPanel>
            <ItemsPanelTemplate>
                <VirtualizingStackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                        Orientation="Vertical"/>
            </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
        <!--<ListBox.ItemTemplate>
            <DataTemplate>
                <local:AreaHorizontalInnerView DataContext="{Binding}" />
            </DataTemplate>
        </ListBox.ItemTemplate>-->

        <ListBox.ItemContainerStyle>
            <Style
       TargetType="ListBoxItem">

                <Setter
           Property="Template">
                    <Setter.Value>
                        <ControlTemplate
                   TargetType="ListBoxItem">
                            <local:AreaHorizontalInnerView DataContext="{Binding}" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="MinWidth" Value="25"/>
                <Setter Property="MinHeight" Value="25"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                <Setter Property="VerticalAlignment" Value="Stretch"/>
            </Style>
        </ListBox.ItemContainerStyle>
    </ListBox>
</UserControl>
