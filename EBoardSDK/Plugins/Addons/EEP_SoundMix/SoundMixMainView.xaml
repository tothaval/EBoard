<UserControl x:Class="EBoardSDK.Plugins.Addons.SoundMix.SoundMixMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             d:DataContext="{d:DesignInstance Type=local:SoundMixMainViewModel}"
             xmlns:local="clr-namespace:EBoardSDK.Plugins.Addons.SoundMix"
             xmlns:controls="clr-namespace:EBoardSDK.Controls.Area"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid MinHeight="150" MinWidth="300">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="SoundMix" FontSize="64" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="{Binding BrushManagement.Foreground, NotifyOnSourceUpdated=True}"
                   Grid.Row="0"/>

        <controls:AreaView DataContext="{Binding AreaViewModel}"
                           Grid.Row="1"/>

    </Grid>
</UserControl>
