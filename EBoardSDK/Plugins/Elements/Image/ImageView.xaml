<!--
// <copyright file="ImageView.xaml" company=".">
// Stephan Kammel
// </copyright>
/// license
///
/// <b>ad-hoc license terms eboard prototype</b><br>
/// <br>
/// <br>
/// contact: kammel@posteo.de
/// <br>
/// <p>
/// until a license has been chosen, you may 
/// use the software or parts of it under the following conditions:<br><br>
/// 1.)
/// If you want to distribute or use the source code or a derived binary
/// of the EBoard project for commercial purposes, you need to contact
/// the project team for authorization and payment details.
/// You may use the source or a derived binary for non commercial 
/// purposes free of charge. In order to do so, copy this adhoc terms
/// and a link to the repository to any source code file that uses code
/// derived from this project and to the folder that holds the compiled source code.
///
/// 2.)
/// THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, 
/// EXPRESS OR IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF
/// MERCHANTABILITY, FITNESS FOR A PARTICULAR PURPOSE AND NONINFRINGEMENT.
/// IN NO EVENT SHALL THE AUTHORS BE LIABLE FOR ANY CLAIM, DAMAGES OR
/// OTHER LIABILITY, WHETHER IN AN ACTION OF CONTRACT, TORT OR OTHERWISE,
/// ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR
/// OTHER DEALINGS IN THE SOFTWARE.
/// </p>
-->
<UserControl x:Class="EBoardSDK.Plugins.Elements.Image.ImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EBoardSDK.Plugins.Elements.Image"
             d:DataContext="{d:DesignInstance Type=local:ImageViewModel}"
             FontSize="{Binding ElementViewModel.FluidUI.Font.FontSize, NotifyOnSourceUpdated=True}"
             FontFamily="{Binding ElementViewModel.FluidUI.Font.FontFamily, NotifyOnSourceUpdated=True, NotifyOnTargetUpdated=True}"
             FontWeight="{Binding ElementViewModel.FluidUI.Font.FontWeight, NotifyOnSourceUpdated=True}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">
    <UserControl.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource DefaultTextBlock}" >
            <Setter Property="Foreground" Value="{Binding ElementViewModel.FluidUI.Design.Foreground, NotifyOnSourceUpdated=True}"/>
        </Style>

        <Style TargetType="TextBox" BasedOn="{StaticResource DefaultTextBox}">
            <Setter Property="Foreground" Value="{Binding ElementViewModel.FluidUI.Design.Foreground, NotifyOnSourceUpdated=True}"/>
            <Setter Property="BorderBrush" Value="{Binding ElementViewModel.FluidUI.Design.Border, NotifyOnSourceUpdated=True}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0 0 10 0"/>
        </Style>

        <Style TargetType="MenuItem" BasedOn="{StaticResource MenuItemStyle}" x:Key="menuitem">
            <Setter Property="BorderBrush" Value="{Binding ElementViewModel.FluidUI.Design.Border}"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="TextBlock.Foreground" Value="{Binding ElementViewModel.FluidUI.Design.Foreground}"/>
            <Setter Property="Foreground" Value="{Binding ElementViewModel.FluidUI.Design.Foreground}"/>
            <Setter Property="Background" Value="{Binding ElementViewModel.FluidUI.Design.Background}"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{Binding ElementViewModel.FluidUI.Design.Highlight}"/>
                </Trigger>

                <Trigger Property="IsMouseOver" Value="False">
                    <Setter Property="Foreground" Value="{Binding ElementViewModel.FluidUI.Design.Foreground}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ToolTip" BasedOn="{StaticResource DefaultToolTip}">
            <Setter Property="Background" Value="{Binding ElementViewModel.FluidUI.Design.Background}"/>
            <Setter Property="BorderBrush" Value="{Binding ElementViewModel.FluidUI.Design.Border}"/>
            <Setter Property="Foreground" Value="{Binding ElementViewModel.FluidUI.Design.Foreground}"/>
            <Setter Property="FontSize" Value="{Binding ElementViewModel.FluidUI.Font.FontSize}"/>
            <Setter Property="FontFamily" Value="{Binding ElementViewModel.FluidUI.Font.FontFamily, NotifyOnSourceUpdated=True, NotifyOnTargetUpdated=True}"/>
            <Setter Property="FontWeight" Value="{Binding ElementViewModel.FluidUI.Font.FontWeight, NotifyOnSourceUpdated=True}"/>
        </Style>
    </UserControl.Resources>

<<<<<<< Updated upstream
    <Grid Background="{Binding ImageBrush}">
        <Grid.ContextMenu>
            <ContextMenu  Background="{Binding ElementViewModel.FluidUI.Design.Background}"
                          BorderBrush="{Binding ElementViewModel.FluidUI.Design.Border}"
                          Style="{StaticResource ContextMenuStyle}"
                          Visibility="{Binding IsImageNotSet, Converter={StaticResource InverseVisibilityToCollapsed}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>

                        <TextBlock Text="Opacity"
                                   Grid.Row="0"/>

                        <TextBox Text="{Binding OpacityValue}"
                                 TextWrapping="NoWrap"
                                 Width="200"
                                 Padding="0 0 15 0"
                                 Grid.Row="1"/>

                        <Slider Value="{Binding OpacityValue}"
                                Background="{Binding ElementViewModel.FluidUI.Design.Background}"
                                Minimum="0"
                                Maximum="1"
                                Width="200"
                                SmallChange="0.05"
                                LargeChange="0.1"
                                Grid.Row="2"/>
                    </Grid>
=======
    <Grid AllowDrop="True"
          Drop="Grid_Drop"
          MinWidth="10"
          MinHeight="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.ContextMenu>
            <ContextMenu Style="{StaticResource ContextMenuStyle}"
                         Background="{Binding ElementViewModel.FluidUI.Design.Background}"
                         BorderBrush="{Binding ElementViewModel.FluidUI.Design.Border}">

                <MenuItem Header="Hide"
                          Style="{StaticResource menuitem}"
                          Visibility="{Binding ContainerIsArea, Converter={StaticResource InverseVisibilityToCollapsed}}">
                    <ToggleButton Content="Hide Element"
                                  IsChecked="{Binding ElementViewModel.HideControl0}"
                                  HorizontalAlignment="Left"/>
                    <ToggleButton Content="Hide Shape"
                                  IsChecked="{Binding ElementViewModel.HideControl1}"
                                  HorizontalAlignment="Left"/>
                </MenuItem>

                <MenuItem Header="Image Size"
                          Style="{StaticResource menuitem}">
                    <Grid Grid.Row="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Ratio"
                       Grid.Row="0"/>

                        <TextBox Text="{Binding Ratio, UpdateSourceTrigger=PropertyChanged, Delay=1000}"
                     Grid.Row="1"/>
                    </Grid>
                </MenuItem>
                
                <MenuItem Header="Image Setup"
                          Visibility="{Binding IsImageNotSet, Converter={StaticResource InverseVisibilityToCollapsed}}"
                          Style="{StaticResource menuitem}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <!-- Stretch -->
                        <TextBlock Text="Image Stretch"
                         Grid.Column="0"
                         Grid.Row="0"/>
                        <ComboBox SelectedItem="{Binding SelectedImageStretch, UpdateSourceTrigger=PropertyChanged}"
                                  
                              x:Name="CbxImageStretch"
                              Grid.Column="1"
                              Grid.Row="0">
                            <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                           Foreground="{Binding ElementViewModel.FluidUI.Design.Foreground}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                        </ComboBox>

                        <!-- Horizontal Alignement -->
                        <TextBlock Text="Horizontal Alignment"
                         Grid.Column="0"
                         Grid.Row="1"/>
                        <ComboBox SelectedItem="{Binding SelectedHorizontalAlignment, UpdateSourceTrigger=PropertyChanged}"                              
                              x:Name="CbxHorizontalAlignment"
                              Grid.Column="1"
                              Grid.Row="1">
                            <!--<ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                     Foreground="Red"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>-->
                        </ComboBox>

                        <!-- Vertical Alignement -->
                        <TextBlock Text="Vertical Alignment"
                         Grid.Column="0"
                         Grid.Row="2"/>
                        <ComboBox SelectedItem="{Binding SelectedVerticalAlignment, UpdateSourceTrigger=PropertyChanged}"                              
                              x:Name="CbxVerticalAlignment"
                              Grid.Column="1"
                              Grid.Row="2">
                            <!--<ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                     Foreground="Red"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>-->
                        </ComboBox>

                        <Grid Grid.Row="3"
                              Grid.Column="0"
                              Grid.ColumnSpan="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto" />
                                <RowDefinition Height="auto" />
                                <RowDefinition Height="auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Text="Opacity"
                       Grid.Row="0"/>

                            <TextBox Text="{Binding OpacityValue}"
                        TextWrapping="NoWrap"
                        Width="200"
                        Padding="0 0 15 0"
                        Grid.Row="1"/>

                            <Slider Value="{Binding OpacityValue}"
                                    Background="{Binding ElementViewModel.FluidUI.Design.Background}"
                                    Minimum="0"
                                    Maximum="1"
                                    Width="200"
                                    SmallChange="0.05"
                                    LargeChange="0.1"
                                    Grid.Row="2"/>
                        </Grid>
                    </Grid>
                </MenuItem>

                <MenuItem Header="Reset"
                          Command="{Binding ResetCommand}"
                          Style="{StaticResource menuitem}"/>
>>>>>>> Stashed changes
            </ContextMenu>
        </Grid.ContextMenu>

        <Grid.ToolTip>
            <ToolTip Content="{Binding LinkTargetPath, NotifyOnSourceUpdated=True}"/>
        </Grid.ToolTip>

        <Grid.InputBindings>
            <MouseBinding Gesture="Control+Leftclick" Command="{Binding ResetCommand}"/>
        </Grid.InputBindings>

        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Grid.ColumnSpan="2"
                   FontWeight="Bold"
                   Text="{Binding Name}"
                   Visibility="{Binding ElementViewModel.HideControl3, Converter={StaticResource InverseVisibilityToCollapsed}}"
                   TextWrapping="Wrap"
                   HorizontalAlignment="Center"/>

        <Button Content="+"
                Command="{Binding SetImageCommand}"
                Style="{StaticResource DefaultButtonStyle}"
                Visibility="{Binding IsImageNotSet, Converter={StaticResource VisibilityToCollapsed}}"
                Grid.Row="1"/>

        <Image Source="{Binding LinkTargetPath}"
               Stretch="{Binding SelectedImageStretch}"
               HorizontalAlignment="{Binding SelectedHorizontalAlignment}"
               VerticalAlignment="{Binding SelectedVerticalAlignment}"
               Opacity="{Binding OpacityValue}"
               Visibility="{Binding IsImageNotSet, Converter={StaticResource InverseVisibilityToCollapsed}}"
               Grid.Row="1"/>
    </Grid>
</UserControl>

<!-- EOF -->