<UserControl x:Class="EBoard.Plugins.Tools.SessionUptimeClock.SessionUptimeClockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EBoard.Plugins.Tools.SessionUptimeClock"
             d:DataContext="{d:DesignInstance Type=local:SessionUptimeClockViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>

        <Style TargetType="Label">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        
    </UserControl.Resources>
    
    <StackPanel Orientation="Vertical">

        <Label Content="{Binding CurrentDate, NotifyOnSourceUpdated=True}"/>
        <Label Content="{Binding CurrentTime, NotifyOnSourceUpdated=True}"/>
        <Label Content="{Binding SessionUptime, NotifyOnSourceUpdated=True}"/>

    </StackPanel>
    
    
</UserControl>
